import 'package:sqflite/sqflite.dart';

class ExperienceRepository {
  // final DatabaseProvider _databaseProvider = DatabaseProvider();

  // Future<void> saveExperience(String title, String organization, String description) async {
  //   final db = await _databaseProvider.database;
  //   await db.insert('experiences', {'title': title, 'organization': organization, 'description': description});
  // }
}
